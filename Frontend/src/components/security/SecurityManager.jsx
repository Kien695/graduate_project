import{useEffect,useState}from"react";import{useDispatch,useSelector}from"react-redux";import{toast}from"react-toastify";import{fetchDevices,fetchAuditLogs,fetchSecurityLevels,logoutDevice}from"../../redux/slices/securitySlice";import StatusBadge from"../common/StatusBadge";import LoadingState from"../common/LoadingState";
const tabs=["Thiết bị đăng nhập","Nhật ký hệ thống","Nhãn bảo mật"];export default function SecurityManager(){const[tab,setTab]=useState(0);const dispatch=useDispatch();const{devices,auditLogs,levels,loading,error}=useSelector(s=>s.security);useEffect(()=>{dispatch(fetchDevices());dispatch(fetchAuditLogs());dispatch(fetchSecurityLevels())},[dispatch]);useEffect(()=>{if(error)toast.error(error)},[error]);const out=async id=>{try{await dispatch(logoutDevice(id)).unwrap();toast.success("Đã đăng xuất thiết bị")}catch{}};return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-800"><div className="flex gap-2 overflow-x-auto border-b border-slate-100 p-4 dark:border-slate-700">{tabs.map((x,i)=><button key={x} onClick={()=>setTab(i)} className={`whitespace-nowrap rounded-xl px-4 py-2.5 text-sm font-bold ${tab===i?"bg-blue-600 text-white":"text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-700"}`}>{x}</button>)}</div>{loading?<LoadingState/>:<div className="overflow-x-auto">{tab===0&&<table className="w-full min-w-[850px] text-left text-sm"><thead className="table-head"><tr>{["Phiên","Thiết bị","Loại","IP đăng nhập","Thời gian","Trạng thái","Thao tác"].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody className="table-body">{devices.map(d=><tr key={d.id}><td className="font-bold">#{d.id}</td><td className="max-w-xs truncate">{d.user_agent||d.device_id||"Không xác định"}</td><td className="capitalize">{d.device_type}</td><td>{d.ip_address||"—"}</td><td>{new Date(d.created_at).toLocaleString("vi-VN")}</td><td><StatusBadge value={d.revoked_at?"inactive":"active"}/></td><td>{!d.revoked_at&&<button className="action-red" onClick={()=>out(d.id)}>Đăng xuất</button>}</td></tr>)}</tbody></table>}{tab===1&&<table className="w-full min-w-[900px] text-left text-sm"><thead className="table-head"><tr>{["Thời gian","Người dùng","Hành động","Đối tượng","Mã đối tượng","IP"].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody className="table-body">{auditLogs.map(a=><tr key={a.id}><td>{new Date(a.created_at).toLocaleString("vi-VN")}</td><td>{a.user_email||`#${a.user_id||"system"}`}</td><td><StatusBadge value={a.action}/></td><td>{a.entity_type}</td><td>{a.entity_id||"—"}</td><td>{a.ip_address||"—"}</td></tr>)}</tbody></table>}{tab===2&&<div className="grid gap-4 p-6 sm:grid-cols-2 xl:grid-cols-4">{levels.map(l=><article key={l.id} className="rounded-2xl border border-slate-200 p-5 dark:border-slate-700"><div className="mb-4 grid h-10 w-10 place-items-center rounded-xl bg-blue-50 font-black text-blue-600 dark:bg-blue-950/50 dark:text-blue-400">{l.rank??l.id}</div><h3 className="font-bold text-slate-900 dark:text-white">{l.name||l.level_name}</h3><p className="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400">{l.description||"Nhãn phân loại truy cập dữ liệu"}</p></article>)}</div>}</div>}</section>}
