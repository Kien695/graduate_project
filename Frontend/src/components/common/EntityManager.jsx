import{useEffect,useMemo,useState}from"react";import{useDispatch,useSelector}from"react-redux";import{toast}from"react-toastify";import Modal from"./Modal";import Pagination from"./Pagination";import StatusBadge from"./StatusBadge";import TableShell from"./TableShell";import LoadingState from"./LoadingState";import{Icon}from"./Icons";
const format=(value,type)=>{if(value===null||value===undefined||value==="")return"—";if(type==="money")return new Intl.NumberFormat("vi-VN",{style:"currency",currency:"VND",maximumFractionDigits:0}).format(Number(value));if(type==="date")return new Date(value).toLocaleDateString("vi-VN");return String(value)};
export default function EntityManager({slice,title,subtitle,columns,fields,thunks,addLabel}){const dispatch=useDispatch();const{items,loading,submitting,error}=useSelector(s=>s[slice]);const[search,setSearch]=useState(""),[page,setPage]=useState(1),[modal,setModal]=useState(false),[editing,setEditing]=useState(null),[form,setForm]=useState({});const pageSize=7;useEffect(()=>{dispatch(thunks.fetch())},[dispatch,thunks]);useEffect(()=>{if(error)toast.error(error)},[error]);const filtered=useMemo(()=>items.filter(item=>columns.some(c=>String(item[c.key]??"").toLowerCase().includes(search.toLowerCase()))),[items,columns,search]);const shown=filtered.slice((page-1)*pageSize,page*pageSize);const open=item=>{setEditing(item||null);setForm(item?Object.fromEntries(fields.map(f=>[f.key,item[f.key]??""])):Object.fromEntries(fields.map(f=>[f.key,f.default??""])));setModal(true)};const submit=async e=>{e.preventDefault();const payload={};for(const f of fields){let value=form[f.key];if(f.type==="number"&&value!=="")value=Number(value);if(f.type==="checkbox")value=Boolean(value);payload[f.key]=value;}try{if(editing)await dispatch(thunks.update({id:editing.id,data:payload})).unwrap();else await dispatch(thunks.create(payload)).unwrap();toast.success(editing?"Cập nhật thành công":"Tạo mới thành công");setModal(false)}catch{} };const remove=async item=>{if(!window.confirm(`Xác nhận xóa ${item.name||item.full_name||item.model||"bản ghi này"}?`))return;try{await dispatch(thunks.remove(item.id)).unwrap();toast.success("Đã xóa dữ liệu")}catch{}};return <><TableShell title={title} subtitle={subtitle} search={search} setSearch={v=>{setSearch(v);setPage(1)}} onAdd={()=>open()} addLabel={addLabel}>{loading?<LoadingState/>:<table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-[11px] uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4">STT</th>{columns.map(c=><th key={c.key} className="px-4 py-4">{c.label}</th>)}<th className="px-5 py-4 text-right">Thao tác</th></tr></thead><tbody className="divide-y divide-slate-100">{shown.map((item,index)=><tr key={item.id} className="hover:bg-slate-50/80"><td className="px-5 py-4 text-xs text-slate-400">{(page-1)*pageSize+index+1}</td>{columns.map(c=><td key={c.key} className={`px-4 py-4 ${c.primary?"font-bold text-slate-900":"text-slate-600"}`}>{c.type==="status"?<StatusBadge value={item[c.key]}/>:format(item[c.key],c.type)}</td>)}<td className="px-5 py-4"><div className="flex justify-end gap-1"><button onClick={()=>open(item)} className="rounded-lg p-2 text-blue-600 hover:bg-blue-50" title="Sửa"><Icon name="edit" className="h-4 w-4"/></button><button onClick={()=>remove(item)} className="rounded-lg p-2 text-rose-500 hover:bg-rose-50" title="Xóa"><Icon name="trash" className="h-4 w-4"/></button></div></td></tr>)}{!shown.length&&<tr><td colSpan={columns.length+2} className="px-5 py-16 text-center text-slate-400">Không có dữ liệu</td></tr>}</tbody></table>}<Pagination page={page} total={filtered.length} pageSize={pageSize} onChange={setPage}/></TableShell><Modal open={modal} onClose={()=>setModal(false)} title={editing?`Cập nhật ${title.toLowerCase()}`:`Thêm ${title.toLowerCase()}`}><form onSubmit={submit} className="grid gap-4 sm:grid-cols-2">{fields.map(f=><label key={f.key} className={f.full?"sm:col-span-2":""}><span className="mb-1.5 block text-xs font-bold text-slate-600">{f.label}{f.required&&" *"}</span>{f.type==="select"?<select required={f.required} value={form[f.key]??""} onChange={e=>setForm({...form,[f.key]:e.target.value})} className="form-control">{f.options.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select>:f.type==="textarea"?<textarea required={f.required} rows="3" value={form[f.key]??""} onChange={e=>setForm({...form,[f.key]:e.target.value})} className="form-control"/>:<input required={f.required} type={f.type||"text"} value={form[f.key]??""} onChange={e=>setForm({...form,[f.key]:e.target.value})} className="form-control"/>}</label>)}<div className="mt-3 flex justify-end gap-3 sm:col-span-2"><button type="button" onClick={()=>setModal(false)} className="rounded-xl border border-slate-200 px-5 py-2.5 text-sm font-semibold">Hủy</button><button disabled={submitting} className="rounded-xl bg-blue-600 px-5 py-2.5 text-sm font-bold text-white disabled:opacity-60">{submitting?"Đang lưu...":"Lưu dữ liệu"}</button></div></form></Modal></>}
